# 准备工作

```
        Mysql >= 5.7.0 (推荐5.7版本)
        oracle >= 12
        Node >= 14
        ​Docker >= 17
        kubernetes >= 1.20.14
        Node-oracledb = 12.1/12.2 (建议12.1)
        chrome >= 99 (建议最新版)
        pm2 >= 5.1.0
        nginx >= 1.17.3
```
